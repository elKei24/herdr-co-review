//! The findings navigator TUI (the right pane of the split-screen).
//!
//! Filled in by the next milestone; this stub keeps the crate compiling and the
//! dispatch wired.

use anyhow::{bail, Result};

use crate::cli::SessionArgs;

pub fn view(_args: &SessionArgs) -> Result<()> {
    bail!("the navigator TUI is not built yet")
}
